
    <%= form_for @confirmed_issue do |f| %>
      <div class="row">
        <div class="s12">
          <%= f.label :title %>
          <%= f.text_field :title,required: true %>
        </div>
      </div>


      <div class="row">
        <div class="col s3">
          <%= f.label :category %>
          <%= f.select(:category, ["Pothole", "Sidewalk", "Streetlight", "Traffic Signal", "Signage",
                          "Road Obstruction", "Pavement", "Powerline", "Vandalism", "Animal Control",
                          "Other"], {}, {:class => "browser-default"})%>
        </div>
        <div class="col s3">
          <%= f.label :danger_level %>
          <%= f.select(:danger_level, ["Mild", "Moderate", "Severe"], {}, {:class => "browser-default"})%>
        </div>
        <div class="col s3">
          <%= f.label :status %>
          <%= f.select(:status, [:pending, :in_progress, :closed, :hidden], {}, {:class => "browser-default"})%>
        </div>
      </div>

      <div class="row">
        <div class="s12">
          <%= f.label :notes %>
          <%= f.text_area :notes %>
        </div>
      </div>

    <% if !@similar_reports.blank? %>
      <div class="row">
        <div class="edit-select">
          <%= f.label :add_similar_reports %>
          <%= f.collection_check_boxes :id, @similar_reports, :id, :report_with_id do |b| %>
            <div class="collection_check_box">
              <%= b.check_box %>
              <%= b.label %>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>


  <%= f.submit "Submit", class: "modal-action modal-close btn waves-effect waves-light"%>
<% end %>
